<!DOCTYPE html>
<html>
  <head>
    <title>Planef</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <% if user_signed_in? %>
      <div id="mySidenav" class="sidenav">
        <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">&times;</a>
        <div>
          <div class="round-image-70">
            <% if current_user.avatar? %>
              <%= image_tag "users/#{current_user.id}/avatar/#{current_user.avatar}", :size => "140x140" %>
            <% else %>
              <%= image_tag "default_teacher_avatar.png", :size => "140x140" %>
            <% end %>
          </div>
          <h3 style = "text-align: center"> (<%= current_user.role %>) </h3>
          <p> <%= link_to "Mi Perfil", user_path(current_user.id) %> </p>
          <p> <%= link_to "Cerrar Sesion", destroy_user_session_path, :method => 'delete' %></p>
          <p> <%= link_to "Colegios", users_schools_path(current_user) %> </p>
        </div>
      </div>
    <% end %>
  <!-- Use any element to open the sidenav -->
    <span onclick="openNav()">open</span>

  <!-- Add all page content inside this div if you want the side nav to push page content to the right (not used if you only want the sidenav to sit on top of the page -->
    <div id="main">
      <%= yield %>
    </div>
  </body>
</html>
