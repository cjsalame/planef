<h1>Editing School</h1>

<%= render 'form', school: @school %>

<%= button_to 'Show', @school, :class=>"links", :method => :get %> |
<%= button_to 'Back', schools_path, :class=>"links", :method => :get %>
